const Pagination = ({ pageLinks }) => {
    pageLinks
    (
        <div>
            {
                pageLinks.map(link =>(
                    Link
                ))
            }
        </div>
    )
}
export default Pagination